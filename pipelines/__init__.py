"""
Pipelines are the top-level entry points for your agentic workflows.

A pipeline:
  1. Accepts external input (CLI args, HTTP trigger, queue message, etc.)
  2. Sets up agents and workflows
  3. Returns a structured result

Each pipeline in pipelines/examples/ is a self-contained, runnable module.
Copy an example and rename it to build your own pipeline.
"""
